# The repo has no .gitignore on main, which makes these easy to commit by
# accident. This branch only.
node_modules/
build/
# The repo builds from yarn.lock. npm install drops a competing lockfile --
# never commit it (see docs 05-decisions.md D-2, D-7).
package-lock.json
.DS_Store
